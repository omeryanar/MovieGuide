<MudStack Spacing="1">
    @if(MediaItems != null)
    {
        foreach(SearchBase item in MediaItems)
        {
            <MudLink Underline="Underline.Hover" Color="Color.Default" Href="@item.Link">
                <MudText Typo="Typo.body2">@item</MudText>
            </MudLink>
        }
    }
</MudStack>

@code
{
    [Parameter]
    public List<SearchBase> MediaItems { get; set; }
}